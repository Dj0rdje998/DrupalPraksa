<?php

function movie_export_controller_theme($existing,$type,$theme,$path){
    
    return [
        'export_list' => array(
            'variables' => array('items' => array(), 'title' => '')
        )
    ];
}